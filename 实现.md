# 企业级 RAG 系统 — 开发文档 vs 实际代码 审查报告

> 审查日期：2026-06-01

---

## 一、总体完成度

按 Phase 0-9 划分：

| Phase | 内容 | 状态 |
|-------|------|------|
| Phase 0 | 项目初始化 | 已完成 |
| Phase 1 | 用户、权限、认证 | 已完成 |
| Phase 2 | 知识库与文档管理 | 已完成 |
| Phase 3 | 文档解析与切分 | 已完成 |
| Phase 4 | 文档审核与发布 | 已完成 |
| Phase 5 | Embedding、Milvus 与检索 | 已完成 |
| Phase 6 | Rerank、Contextual Retrieval、RAG 问答 | 已完成 |
| Phase 7 | LangGraph 编排与流式响应 | 已完成 |
| Phase 8 | 后台配置、审计、反馈 | 大部分完成（~80%） |
| Phase 9 | RAG 评测与运维 | 部分完成（~60%） |

**整体评估：Phase 0-7 基本完整，Phase 8-9 有缺口。**

---

## 二、逐模块对比详情

### 2.1 认证系统（文档 §6.1, §19.1）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| POST /api/auth/login | 已完成 | `backend/app/api/auth.py:22` |
| POST /api/auth/logout | 已完成 | `backend/app/api/auth.py:105` |
| POST /api/auth/refresh | 已完成 | `backend/app/api/auth.py:82` |
| GET /api/auth/me | 已完成 | `backend/app/api/auth.py:111` |
| POST /api/auth/register | 已完成 | `backend/app/api/auth.py:59` |
| JWT access_token (30min) + refresh_token (7天) | 已完成 | `backend/app/core/security.py:27-45` |
| bcrypt 密码哈希 | 已完成 | `backend/app/core/security.py:18-24` |
| 登录限流 (用户名5次/min, IP 20次/min) | 已完成 | `backend/app/core/rate_limit.py:27-45` |

### 2.2 用户与权限（文档 §6, §19.2, §19.3）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 5角色体系 (SuperAdmin/Admin/KBAdmin/Reviewer/User) | 已完成 | 种子数据 |
| 9权限码 RBAC | 已完成 | `backend/app/core/security.py:90-100` |
| require_role / require_permission 守卫 | 已完成 | `backend/app/core/security.py:77-100` |
| ABAC (部门/用户覆盖) | 已完成 | `knowledge_bases.py` 权限查询 |
| 用户 CRUD | 已完成 | `backend/app/api/users.py` |
| 个人RAG开关 (toggle) | 已完成 | `backend/app/api/users.py:143` |
| 修改密码 | 已完成 | `backend/app/api/users.py:170` |
| 部门 CRUD + 成员管理 | 已完成 | `backend/app/api/departments.py` |
| 角色权限查询 | 已完成 | `backend/app/api/roles.py` |
| 前端登录/注册页 | 已完成 | `frontend/app/login/`, `frontend/app/register/` |
| 前端用户管理页 | 已完成 | `frontend/app/users/` |
| 前端部门管理页 | 已完成 | `frontend/app/departments/` |
| 前端权限管理页 | 已完成 | `frontend/app/permissions/` |
| 前端侧栏权限过滤 | 已完成 | `frontend/components/layout/admin-layout.tsx` |

### 2.3 知识库管理（文档 §4, §19.4）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 知识库 CRUD | 已完成 | `backend/app/api/knowledge_bases.py` |
| 企业/个人类型隔离 | 已完成 | 列表查询按 type + owner 过滤 |
| KB 权限 (view/query/upload/review/publish/manage/delete) | 已完成 | `knowledge_base_permissions` 表 |
| 用户覆盖 (allow/deny) | 已完成 | `user_kb_overrides` 表 |
| KB 级别 RAG 配置 | 已完成 | `GET/PATCH /api/knowledge-bases/{id}/rag-config` |
| 知识库删除 (级联清理 MinIO + Milvus) | 已完成 | `knowledge_bases.py:97-133` |
| 前端知识库管理页 | 已完成 | `frontend/app/knowledge-bases/` |

### 2.4 文档管理（文档 §7, §19.5）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 上传文档 (6种格式, 100MB限制) | 已完成 | `backend/app/api/documents.py:20-94` |
| 文档列表 (按KB过滤) | 已完成 | `backend/app/api/documents.py:173-199` |
| 文档详情 + 版本列表 | 已完成 | `backend/app/api/documents.py:202-262` |
| 文档预览 (MinIO presigned URL) | 已完成 | `backend/app/api/documents.py:265-280` |
| Chunk 预览 | 已完成 | `backend/app/api/documents.py:314-341` |
| 文档替换 (hash检测+新版本) | 已完成 | `backend/app/api/documents.py:97-170` |
| 文档审核 (通过/驳回) | 已完成 | `backend/app/api/documents.py:356-401` |
| 文档发布 (停用旧版本chunks) | 已完成 | `backend/app/api/documents.py:404-443` |
| 文档下架 | 已完成 | `backend/app/api/documents.py:446-468` |
| 重试失败文档 | 已完成 | `backend/app/api/documents.py:471-498` |
| 重建索引 | 已完成 | `backend/app/api/documents.py:501-536` |
| 文档重命名 | 已完成 | `backend/app/api/documents.py:539-554` |
| 文档删除 (级联清理) | 已完成 | `backend/app/api/documents.py:557-587` |
| 前端文档管理页 | 已完成 | `frontend/app/documents/` |
| 前端审核页 | 已完成 | `frontend/app/review/` |

### 2.5 文档解析与切分（文档 §8, §9）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| TXT/MD/PDF/DOCX/XLSX/PPTX 解析 | 已完成 | `backend/app/services/file_parser.py` |
| 统一 Block 中间结构 | 已完成 | `backend/app/services/file_parser.py:14-23` |
| Parent-Child Chunking | 已完成 | `backend/app/services/chunking.py` |
| 按文件类型切分 | 已完成 | chunking.py 中按类型处理 |
| Docker Worker 异步处理 parse+chunk | 已完成 | `backend/app/workers/main.py` |
| 文档生命周期状态流转 | 已完成 | uploaded→parsing→pending_review |
| **chunking 状态** | **跳过** | worker 直接从 parsing 到 pending_review |

### 2.6 Embedding、Milvus 与检索（文档 §5, §12, §19.6）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| bge-m3 embedding (本地, dim=1024) | 已完成 | `backend/app/services/embedding_service.py` |
| Milvus 向量存储 + 索引 | 已完成 | `backend/app/services/milvus_service.py` |
| 按 document_id 删除向量 | 已完成 | `backend/app/services/milvus_service.py:100-111` |
| PostgreSQL pg_trgm 关键词检索 | 已完成 | `backend/app/services/retrieval_service.py:16-76` |
| RRF 融合 | 已完成 | `backend/app/services/retrieval_service.py:79-102` |
| 混合检索 (向量+关键词) | 已完成 | `backend/app/services/retrieval_service.py:105-128` |
| 检索时 is_active 过滤 | 已完成 | retrieval_service.py SQL 中 `c.is_active = true` |
| index_worker (本地运行, torch) | 已完成 | `backend/app/services/index_worker.py` |
| Docker worker 跳过 embed 任务 | 架构设计 | worker 标记为 completed，需本地 index_worker |

### 2.7 Rerank、Contextual Retrieval 与问答（文档 §10, §11, §14）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| bge-reranker-v2-m3 精排 | 已完成 | `backend/app/services/rerank_service.py` |
| Contextual Retrieval (chunk 上下文) | 已完成 | `backend/app/services/contextual_retrieval.py` |
| Query Rewrite | 已完成 | `backend/app/services/query_rewrite.py` |
| Parent Chunk 回填 | 已完成 | `backend/app/services/retrieval_service.py:159-181` |
| 低置信度拒答 | 已完成 | score_threshold 判断 + 知识缺口记录 |
| 来源引用 + 原文片段 | 已完成 | `build_sources()` 输出 |
| LLM 统一调用 (DB配置优先, .env回退) | 已完成 | `backend/app/services/llm_service.py` |
| 流式生成 (SSE) | 已完成 | `backend/app/services/llm_service.py` |
| 企业 RAG 非流式 + 流式 | 已完成 | `backend/app/api/enterprise_rag.py` |
| 个人 RAG 非流式 + 流式 | 已完成 | `backend/app/api/personal_rag.py` |
| 答案规范 (结论→依据→分析→风险→引用) | 已完成 | System prompt 中定义 |
| **Contextual Retrieval 实际集成到入库流程** | **未集成** | embedding 时未调用 contextual_retrieval |

### 2.8 LangGraph 编排（文档 §13）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| StateGraph 工作流 | 已完成 | `backend/app/services/langgraph_workflow.py:156-186` |
| 节点: rewrite→retrieve→rerank→confidence→expand→generate | 已完成 | 同文件 |
| 流式 astream_events | 已完成 | `run_rag_stream()` |
| 企业RAG + 个人RAG 双链路 | 已完成 | 通过 kb_ids 区分 |
| ERP Tool 分支预留 | **未实现** | LangGraph 中无 ERP 分支 |

### 2.9 会话与反馈（文档 §19.12, §24）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 会话列表/详情/删除 | 已完成 | `backend/app/api/sessions.py` |
| 会话自动保存 | 已完成 | `_save_session()` |
| 消息反馈 (like/dislike) | 已完成 | `backend/app/api/sessions.py:103-123` |
| 反馈原因记录 | 部分 | 追加到 message.content，非独立字段 |

### 2.10 模型配置（文档 §16, §19.8）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 模型 CRUD | 已完成 | `backend/app/api/model_configs.py` |
| 多供应商 (OpenAI/Claude/DeepSeek/Qwen/本地) | 已完成 | OpenAI 兼容协议 |
| is_default 互斥 | 已完成 | 创建/更新时自动处理 |
| API Key 加密存储 | **未验证** | 字段存在 `api_key_encrypted`，实际存储未加密 |
| 前端模型配置页 | 已完成 | `frontend/app/model-configs/` |
| 每日配额 (daily_quota) | **未实现** | ModelConfigRequest 中无此字段 |
| allowed_role_ids | **未实现** | 前端/API 未暴露 |

### 2.11 RAG 参数配置（文档 §17, §19.9）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| KB级别 RAG 配置 CRUD | 已完成 | `knowledge_bases.py:164-207` |
| 全局 RAG 配置 CRUD API | **缺失** | 无 `/api/admin/rag-configs` 独立端点 |
| 前端 RAG 配置页 | 已完成 | `frontend/app/rag-configs/` |
| 配置项完整性 | 已完成 | chunk_size/overlap/top_k/rrf_k/rerank/score_threshold 等 |

### 2.12 审计日志（文档 §22, §19.10）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 审计日志查询 | 已完成 | `backend/app/api/audit_logs.py` |
| security_log (登录/权限) | **未覆盖** | 登录未写审计日志 |
| operation_log (上传/审批/发布) | **未覆盖** | 文档操作未写审计日志 |
| rag_trace_log (问答/检索) | 部分覆盖 | 仅 enterprise_rag 写了 rag_query |
| RAG Trace 日志独立 API | **缺失** | 无 `/api/rag-trace-logs` 端点 |
| 前端审计日志页 | 已完成 | `frontend/app/audit-logs/` |

### 2.13 知识缺口（文档 §24, §19.11）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 低置信度自动记录缺口 | 已完成 | `enterprise_rag.py:158`, `personal_rag.py:144` |
| 缺口列表/更新/关闭 | 已完成 | `backend/app/api/knowledge_gaps.py` |
| 状态流转 (open/processing/resolved/ignored) | 已完成 | 支持 status 更新 |
| 前端知识缺口页 | 已完成 | `frontend/app/knowledge-gaps/` |

### 2.14 评测体系（文档 §25, §19.13）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 评测集 CRUD | 已完成 | `backend/app/api/evaluations.py:29-71` |
| 评测运行 (后台异步) | 已完成 | `backend/app/api/evaluations.py:92-114` |
| 评测结果查看 | 已完成 | `backend/app/api/evaluations.py:117-144` |
| 前端评测页 | 已完成 | `frontend/app/evaluations/` |
| **评测对比 (不同RAG配置)** | **未实现** | 无配置对比功能 |
| **幻觉率评估** | **未实现** | 指标中无此项 |

### 2.15 监控运维（文档 §26）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 基础指标 (计数器+耗时) | 已完成 | `backend/app/services/metrics_service.py` |
| 监控 API | 已完成 | `backend/app/api/monitor.py` |
| 前端监控页 | 已完成 | `frontend/app/monitor/` |
| Prometheus + Grafana | **第二阶段** | 文档标注为 Phase 9+ |
| 日志文件分类 (6类) | **未实现** | 仅 Python logging 到 stdout |

### 2.16 限流与额度（文档 §23）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 登录限流 | 已完成 | `backend/app/core/rate_limit.py` |
| 用户问答限流 (20次/min) | **未实现** | 无中间件 |
| IP 请求限流 (60次/min) | **未实现** | 无中间件 |
| 单文件上传限制 (100MB) | 已完成 | documents.py MAX_FILE_SIZE |
| 用户总上传容量 (5GB) | **未实现** | |
| 昂贵模型每日配额 | **未实现** | |

### 2.17 个人 RAG（文档 §3.2, §11.2, §19.7）

| 文档要求 | 实现情况 | 文件 |
|----------|----------|------|
| 个人RAG问答 (流式+非流式) | 已完成 | `backend/app/api/personal_rag.py` |
| 个人知识库隔离 (owner_user_id) | 已完成 | `_resolve_personal_kb_ids()` |
| 个人RAG开关控制 | 已完成 | `personal_rag_enabled` 字段 |
| 会话历史 | 已完成 | session CRUD 复用 |
| **手动笔记 CRUD** | **未实现** | 无 `/api/personal-rag/notes` 端点 |
| 前端个人RAG问答页 | 已完成 | `frontend/app/personal-rag/` |

### 2.18 ERP 预留（文档 §3.3）

| 文档要求 | 实现情况 |
|----------|----------|
| `backend/app/services/erp_service.py` | **不存在** |
| `backend/app/tools/erp_tools.py` | **不存在** |
| LangGraph ERP 分支 | **未实现** |

### 2.19 数据库表（文档 §18）

| 表 | 实现情况 | 备注 |
|----|----------|------|
| users, departments, roles, permissions | 已完成 | |
| user_roles, department_members | 已完成 | |
| knowledge_bases | 已完成 | |
| knowledge_base_permissions, user_kb_overrides | 已完成 | |
| documents, document_versions | 已完成 | |
| document_processing_tasks | 已完成 | |
| **document_permissions** | **未实现** | 文档18.3列出但无此表 |
| chunks | 已完成 | 缺少部分字段 (见下方) |
| chat_sessions, chat_messages | 已完成 | |
| rag_answer_sources | 已完成 | |
| model_configs | 已完成 | daily_quota/allowed_role_ids 字段未用 |
| rag_configs | 已完成 | |
| audit_logs | 已完成 | |
| **rag_trace_logs** | **未实现** | 无独立 trace 表 |
| knowledge_gaps | 已完成 | |
| **rate_limit_rules** | **未实现** | 无动态限流规则表 |
| **user_usage_stats** | **未实现** | |

### 2.20 Chunk 表字段对比（文档 §18.4）

文档列出了 21 个字段，实际实现情况：

| 字段 | 状态 |
|------|------|
| id, chunk_id, parent_chunk_id | 已完成 |
| document_id, document_version_id | 已完成 |
| knowledge_base_id | 已完成 |
| **tenant_id** | 未实现 |
| **department_id** | 未实现 |
| **owner_user_id** | 未实现 |
| chunk_index, chunk_text | 已完成 |
| **contextual_text** | 未实现 (contextual_retrieval 模块存在但结果未存储到DB) |
| page_no, sheet_name, slide_no | 已完成 |
| section_title, token_count | 已完成 |
| chunk_hash | 已完成 |
| **milvus_vector_id** | 未实现 (入库后未回写) |
| **visibility** | 未实现 |
| **allowed_user_ids, denied_user_ids** | 未实现 |
| **allowed_role_ids, allowed_department_ids** | 未实现 |
| is_active, document_status | 已完成 |
| created_at | 已完成 |

### 2.21 前端页面（文档 §15.1, §21）

文档列出 16 个页面，实际实现 18 个路由：

| 文档页面 | 实现状态 |
|----------|----------|
| 登录页 | 已完成 `login/` |
| 首页/工作台 | 已完成 `dashboard/` + `page.tsx` |
| 企业RAG问答页 | 已完成 `enterprise-rag/` |
| 个人RAG问答页 | 已完成 `personal-rag/` |
| 知识库管理页 | 已完成 `knowledge-bases/` |
| 文档管理页 | 已完成 `documents/` |
| 文档审核页 | 已完成 `review/` |
| 来源预览页 | 内嵌在聊天组件 `source-card.tsx` |
| 用户管理页 | 已完成 `users/` |
| 部门管理页 | 已完成 `departments/` |
| 权限管理页 | 已完成 `permissions/` |
| 模型配置页 | 已完成 `model-configs/` |
| 审计日志页 | 已完成 `audit-logs/` |
| 知识缺口管理页 | 已完成 `knowledge-gaps/` |
| 评测管理页 | 已完成 `evaluations/` |
| **系统设置页** | **未实现** `settings/` 目录不存在 |
| 额外: RAG配置页 | 已完成 `rag-configs/` |
| 额外: 监控页 | 已完成 `monitor/` |
| 额外: 注册页 | 已完成 `register/` |

---

## 三、需要实现的内容（按优先级排列）

### 高优先级（影响核心功能）

1. **个人RAG笔记 CRUD** — 文档 §19.7 明确要求 `POST/GET/PATCH/DELETE /api/personal-rag/notes`
   - 需新建 notes 表（或复用 documents 表加 type=note）
   - 后端新增 personal_rag.py 中的 notes 端点
   - 前端个人RAG页添加笔记编辑入口

2. **审计日志全覆盖** — 目前仅记录 rag_query
   - 登录/登出记录
   - 文档上传/删除/审批/发布记录
   - 权限修改记录
   - 模型配置修改记录

3. **API Key 加密存储** — 文档 §31.5 明确要求
   - model_configs 表的 api_key_encrypted 字段需真正加密
   - 读取时解密，写入时加密
   - 日志/API 返回中脱敏（当前已用 `***` 脱敏）

4. **问答/上传限流** — 文档 §23
   - 用户问答 20次/min
   - 上传大小限制已实现，缺少每日上传数量限制

### 中优先级（完善功能）

5. **Contextual Retrieval 集成到入库流程**
   - `contextual_retrieval.py` 模块已存在但 index_worker 未调用
   - 需要在 embedding 前为 chunk 生成 contextual_text
   - 对 contextual_text 做 embedding 而非原始 chunk_text

6. **RAG Trace 日志** — 文档 §19.10
   - 新建 rag_trace_logs 表
   - 在检索链路各阶段记录耗时、召回数、rerank 分数
   - 新增 `/api/rag-trace-logs` 查询端点

7. **Chunk 表字段补充**
   - milvus_vector_id 回写（入库后更新）
   - contextual_text 存储
   - visibility 字段（用于 chunk 级权限控制）

8. **定时清理旧向量任务** — 文档 §7.3
   - 定时扫描 is_active=false 的旧版本 chunk
   - 从 Milvus 删除对应向量

### 低优先级（锦上添花）

9. **ERP 预留文件** — 文档 §3.3
   - 创建 `backend/app/services/erp_service.py` (空实现)
   - 创建 `backend/app/tools/erp_tools.py` (空实现)
   - LangGraph 中添加 erp_tool_branch 占位

10. **全局 RAG 配置独立 API** — 文档 §19.9
    - 新增 `/api/admin/rag-configs` CRUD（区别于 KB 级别的 rag-config）

11. **系统设置页** — 文档 §15.1
    - 前端新增 `frontend/app/settings/` 页面

12. **模型配置增强**
    - daily_quota 每日调用额度
    - allowed_role_ids 允许使用的角色

13. **document_permissions 表** — 文档 §18.3
    - 文档级独立权限控制

14. **评测增强**
    - 不同 RAG 配置对比评测
    - 幻觉率指标

15. **备份恢复方案文档** — 文档 §27
    - 编写备份策略文档（不必须自动化）

---

## 四、关键架构决策已落实

以下设计要点代码中已正确实现：

- RBAC + ABAC 混合权限模型
- 文档生命周期 `uploaded→parsing→pending_review→approved→published`
- 内容 hash 检测避免重复版本
- 发布时停用旧版本 chunks (is_active = false)
- 检索时 `is_active = true` 过滤
- 企业 RAG 与个人 RAG 物理隔离
- DB 级 CASCADE 删除 + MinIO/Milvus 清理
- LangGraph 编排 + SSE 流式
- 向量 + PostgreSQL Full Text Search + RRF + Rerank 完整链路
- 低置信度拒答 + 知识缺口自动记录
- Docker worker 处理 parse/chunk，本地 index_worker 处理 embedding（架构原因）

# RAG 系统整体思路 & 代码流转路径

> 企业级 RAG 知识库问答系统，涵盖文档管理、混合检索、权限控制、监控运维完整链路。

---

## 项目结构

```
RAGSystem/
├── backend/app/
│   ├── api/            # FastAPI 路由层
│   ├── core/           # 配置、安全、限流
│   ├── db/models/      # SQLAlchemy 数据模型
│   ├── schemas/        # Pydantic 请求/响应模型
│   ├── services/       # 业务逻辑层
│   └── workers/        # 异步任务（解析/Embedding）
├── frontend/
│   ├── app/            # Next.js 14 App Router 页面
│   ├── components/     # React 组件
│   ├── lib/            # 前端工具库（API、Auth、SSE）
│   └── types/          # TypeScript 类型定义
└── models/             # bge-m3 embedding 模型文件
```

---

## 1. 认证链路

```
前端登录 → JWT 签发 → 请求拦截 → 权限守卫
```

| 步骤 | 位置 | 说明 |
|------|------|------|
| 用户登录 | `frontend/app/login/page.tsx` → `handleSubmit()` | 调用 `login()` |
| 发送凭证 | `frontend/lib/auth.ts` → `login()` | `POST /api/auth/login` |
| 限流检查 | `backend/app/core/rate_limit.py` → `check_login_rate()` | Redis 5次/分(用户) 20次/分(IP) |
| 密码验证 | `backend/app/api/auth.py` → `login()` | bcrypt 验证 |
| 签发令牌 | `backend/app/core/security.py` → `create_access_token()` | jose.jwt HS256，Access 30min + Refresh 7d |
| 前端存储 | `frontend/lib/auth.ts` | localStorage: `access_token` + `refresh_token` |
| 请求注入 | `frontend/lib/api.ts` → `apiFetch()` | `Authorization: Bearer <token>` 自动注入 |
| 过期刷新 | `frontend/lib/auth.ts` → `refreshToken()` | `/api/auth/refresh`，前端解码 JWT 检查过期 |
| 身份解析 | `backend/app/core/security.py` → `get_current_user()` | FastAPI Depends，解码 JWT + 加载 User.roles |
| 用户信息 | `backend/app/api/auth.py` → `me()` `GET /api/auth/me` | 返回 id、username、department_id、roles |
| 前端上下文 | `frontend/lib/auth-context.tsx` → `AuthProvider` | 角色→权限本地推导，`useAuth()` 全局可用 |
| 角色守卫 | `backend/app/core/security.py` → `require_role("SuperAdmin")` | 检查 user.roles 交集 |
| 权限守卫 | `backend/app/core/security.py` → `require_permission("upload_document")` | 遍历 role.permissions 匹配 code |

**9 个系统权限码：** `manage_user` / `manage_department` / `manage_knowledge_base` / `upload_document` / `review_document` / `publish_document` / `query_knowledge_base` / `manage_model_config` / `view_audit_logs`

---

## 2. RAG 问答链路（企业）

```
问题输入 → Query Rewrite → 混合检索 → RRF 融合 → Rerank → 置信度检测 → Parent Chunk 扩展 → LLM 流式生成 → 来源展示 → 会话持久化
```

### 2.1 前端发送

| 步骤 | 位置 |
|------|------|
| 用户输入问题 | `frontend/app/enterprise-rag/page.tsx` → `handleSend()` |
| SSE 流式请求 | `frontend/lib/stream.ts` → `streamChat("/api/enterprise-rag/chat/stream", ...)` |
| 解析 SSE 事件 | `stream.ts` 读取 `ReadableStream`，按行解析 `event:` + `data:` |
| 渲染消息 | `frontend/components/chat/chat-panel.tsx` → `ChatPanel` 组件 |
| 思考块折叠 | `frontend/components/chat/think-block.tsx` → `parseThinkBlocks()` + `ThinkBlock` |

### 2.2 后端入口

| 步骤 | 位置 |
|------|------|
| API 端点 | `backend/app/api/enterprise_rag.py` → `chat_stream()` `POST /api/enterprise-rag/chat/stream` |
| KB 权限过滤 | `backend/app/services/kb_access.py` → `get_accessible_kb_ids(user, "query", db)` |
| 读取 RAG 配置 | `backend/app/services/retrieval_service.py` → `get_rag_configs(kb_ids)` |
| 流式响应 | `StreamingResponse(generate(), media_type="text/event-stream")` |

### 2.3 LangGraph 编排

| 步骤 | 位置 |
|------|------|
| 工作流入口 | `backend/app/services/langgraph_workflow.py` → `run_rag_stream()` |
| StateGraph 节点 | `rewrite → retrieve → rerank → check_confidence → [expand`&#124;`reject]` |
| 事件监听 | `graph.astream_events(initial_state, version="v2")` → 拦截 `on_chain_start/end` |
| 指标记录 | `backend/app/services/metrics_service.py` → `increment_counter()` / `record_timing()` |

### 2.4 Query Rewrite（查询改写）

| 步骤 | 位置 |
|------|------|
| 改写节点 | `langgraph_workflow.py` → `_make_rewrite_node()` |
| 改写逻辑 | `backend/app/services/query_rewrite.py` → `rewrite_query()` |
| 复合检测 | 正则匹配 `和/与/以及/分别/各` 判断是否复合问题 |
| 拆分/改写 | 复合问题 → 拆分多子问题；普通问题 → 改写 2-3 个变体 |
| 清理输出 | `re.sub(r'<think>.*?</think>', '', result)` 去除 thinking 模型标签 |

### 2.5 混合检索（Hybrid Search）

| 步骤 | 位置 |
|------|------|
| 检索节点 | `langgraph_workflow.py` → `_make_retrieve_node()` |
| 混合检索入口 | `backend/app/services/retrieval_service.py` → `hybrid_search()` |
| 向量检索 | `backend/app/services/milvus_service.py` → `search()` → Milvus `rag_chunks` 集合，COSINE 相似度，`expr` 过滤 KB |
| Embedding | `backend/app/services/embedding_service.py` → `embed_query()` → bge-m3 (1024维) |
| 关键词检索 | `retrieval_service.py` → `_pg_keyword_search()` → PostgreSQL `pg_trgm` 扩展，`similarity()` + `ILIKE` |
| RRF 融合 | `retrieval_service.py` → `_rrf_fusion()` → 公式 `1/(k+rank+1)` k=60 |
| 结果补全 | `retrieval_service.py` → `_enrich_results()` → 补 `document_name`、`parent_chunk_id` |

### 2.6 Rerank（重排序）

| 步骤 | 位置 |
|------|------|
| 重排节点 | `langgraph_workflow.py` → `_make_rerank_node()` |
| 重排逻辑 | `backend/app/services/retrieval_service.py` → `rerank_results()` |
| Cross-encoder | `backend/app/services/rerank_service.py` → `rerank()` → bge-reranker-v2-m3 `FlagReranker.compute_score()` |
| 截断 | 取 `rerank_top_n` 条（默认 6） |

### 2.7 置信度检测 + Parent Chunk 扩展

| 步骤 | 位置 |
|------|------|
| 置信度节点 | `langgraph_workflow.py` → `_make_confidence_node()` |
| 判定逻辑 | `max_score < score_threshold`（默认 0.45）→ `low_confidence=True` |
| 扩展节点 | `langgraph_workflow.py` → `_make_expand_node()` |
| Parent 回填 | `retrieval_service.py` → `expand_parent_chunks()` → 按 `parent_chunk_id` 查询父块文本 |
| 上下文拼接 | `retrieval_service.py` → `build_context()` → 格式化为 LLM 输入 |
| 来源构建 | `retrieval_service.py` → `build_sources()` → 提取文档名、分数、片段 |

### 2.8 LLM 流式生成

| 步骤 | 位置 |
|------|------|
| 模型选择 | `backend/app/services/llm_service.py` → `_get_db_config()` → 优先 DB `model_configs` 表 `is_default=true`，回退 `.env` |
| 客户端创建 | `llm_service.py` → `_get_llm()` → `langchain_openai.ChatOpenAI`，自动拼 `base_url`（DeepSeek/Qwen/OpenAI） |
| 流式生成 | `llm_service.py` → `generate_stream()` → `llm.astream()` 异步生成器 |
| Token 输出 | `langgraph_workflow.py` → `async for chunk in llm.generate_stream()` → `yield {"type":"answer","content":chunk}` |

### 2.9 SSE 事件类型

| 事件 | 内容 |
|------|------|
| `event: status` | `{type, node, message}` — 节点进度（正在改写/检索/重排/评估/加载） |
| `event: answer` | `{type, content}` — LLM token 片段 |
| `event: sources` | `{type, content}` — 最终来源列表 `[{document_name, chunk_text, score, ...}]` |
| `event: done` | `{session_id, low_confidence}` — 完成信号 |
| `event: error` | `{type, content}` — 错误信息 |

### 2.10 会话持久化 + 审计 + 知识缺口

| 步骤 | 位置 |
|------|------|
| 保存会话 | `enterprise_rag.py` → `_save_session()` → `ChatSession` + `ChatMessage`(user+assistant) + `RagAnswerSource`(≤10条) |
| 审计日志 | `backend/app/services/audit_service.py` → `log(db, "rag_query", ...)` → `AuditLog` 表 |
| 知识缺口 | 若 `low_confidence=True` → 写入 `KnowledgeGap(question, user_id, session_id)` |

---

## 3. RAG 问答链路（个人）

与企业的差异：

| 差异点 | 企业 | 个人 |
|--------|------|------|
| API | `enterprise_rag.py` | `personal_rag.py` |
| KB 来源 | 所有活跃企业 KB（经权限过滤） | 当前用户拥有的 `type=personal` KB |
| Query Rewrite | 默认开启 | 默认关闭 |
| 功能开关 | 无需检查 | 检查 `current_user.personal_rag_enabled` |

---

## 4. 文档生命周期

```
上传 → 解析(Worker) → 分块(Parent-Child) → 审核(通过/驳回) → 发布 → Embedding入库(Worker)
```

| 阶段 | 位置 | 核心逻辑 |
|------|------|----------|
| **上传** | `backend/app/api/documents.py` → `upload_document()` `POST /api/documents/upload` | MinIO 存储 → 创建 `Document` + `DocumentVersion` → 创建 `parse` 任务 |
| **解析** | `backend/app/workers/main.py` → `process_parse_task()` | 下载文件 → `file_parser.py` 解析(txt/md/pdf/docx/xlsx/pptx) → 产出 `Block` 列表 |
| **分块** | `backend/app/services/chunking.py` → `chunk_blocks()` | Child: 滑动窗口 700 token/100 重叠 → Parent: 合并子块 1600 token/250 重叠 |
| **Hash** | `chunking.py` | 每块 `chunk_hash = sha256(chunk_text)[:16]` 用于增量索引 |
| **审核** | `documents.py` → `review_document()` `POST /api/documents/{id}/review` | `pending_review → approved`&#124;`rejected` |
| **发布** | `documents.py` → `publish_document()` `POST /api/documents/{id}/publish` | 旧版 chunk `is_active=False` → 当前 chunk `is_active=True` → 创建 `embed` 任务 |
| **Embed** | `workers/main.py` → `process_embed_task()` | 查询活跃 chunk → hash 匹配复用 → 新 chunk 调用 bge-m3 → 写入 Milvus |

---

## 5. 增量索引链路

复用未变更 chunk 的向量，避免重复 embedding。

| 步骤 | 位置 |
|------|------|
| 查询旧向量 | `milvus_service.py` → `get_vectors_by_document_id(doc_id)` |
| 构建复用映射 | `workers/main.py` → `reuse_map = {chunk_hash: embedding}` |
| 删除旧向量 | `milvus_service.py` → `delete_by_document_id(doc_id)` |
| 分离新旧 | `reuse_chunks`（hash 命中）vs `new_chunks`（hash 未命中） |
| 仅 embed 新块 | `embedding_service.embed_texts()` 只对 `new_chunks` 调用 |
| 合并写入 | 复用向量 + 新向量 → `milvus_service.insert_chunks()` |

---

## 6. Contextual Retrieval 链路

为每个 chunk 生成 100-200 字的文档上下文描述，拼接后一起 embedding。

| 步骤 | 位置 |
|------|------|
| 入口 | `workers/main.py` → `process_embed_task()` 检查 `RAGConfig.enable_contextual_retrieval` |
| 上下文生成 | `backend/app/services/contextual_retrieval.py` → `generate_chunk_context()` |
| 并发控制 | `asyncio.Semaphore(5)` |
| 拼接策略 | `contextual_text + "\n\n" + chunk_text` 作为 embedding 输入 |
| 复用优化 | 已有 `contextual_text` 的 chunk（复用场景）跳过 LLM 调用 |

---

## 7. 系统权限链路（RBAC）

```
User ──[user_roles]──→ Role ──[role_permissions]──→ Permission
```

| 步骤 | 位置 |
|------|------|
| 数据模型 | `backend/app/db/models/user.py` + `role.py` + `associations.py` |
| 种子数据 | `backend/app/db/seed.py` → 5 角色 + 9 权限 |
| 后端守卫 | `backend/app/core/security.py` → `require_role()` / `require_permission()` |
| 前端推导 | `frontend/lib/auth-context.tsx` → `derivePermissions()` 本地映射 |
| 前端使用 | `useAuth()` → `hasRole()` / `hasPermission()` 控制 UI 显隐 |

---

## 8. KB 级权限链路

```
KnowledgeBasePermission(user_id | department_id | role_id, permission_type)  +  UserKBOverride(user_id, allow|deny)
```

| 步骤 | 位置 |
|------|------|
| 解析入口 | `backend/app/services/kb_access.py` → `get_accessible_kb_ids(user, "query", db)` |
| 管理员放行 | `"SuperAdmin" in role_names or "Admin" in role_names` → 返回全部 KB |
| 收集部门 ID | `user.department_id`(FK) + `department_members`(M2M) 两路合并 |
| 匹配条件 | `OR(user_id = ?, department_id IN ?, role_id IN ?)` 查询 `KnowledgeBasePermission` |
| 无权限 KB | 未配置任何 `KnowledgeBasePermission` 的 KB → 全员开放 |
| 覆盖生效 | `UserKBOverride`: `deny` 剔除 / `allow` 放行 |
| API 暴露 | `knowledge_bases.py` → `GET /accessible`、`GET/DELETE /{kb_id}/permissions`、`GET/DELETE /{kb_id}/user-overrides` |

---

## 9. 部门-用户-权限三者连接

```
User ──department_id(FK)──→ Department
User ──department_members(M2M)──→ Department
Department ──KnowledgeBasePermission.department_id──→ KB 访问权限
```

| 步骤 | 位置 |
|------|------|
| 成员管理 | `backend/app/api/departments.py` → `add_member()` / `remove_member()` (M2M) |
| 用户部门 | `backend/app/api/users.py` → 创建/更新时接受 `department_id` (FK) |
| 访问解析 | `backend/app/services/kb_access.py` → 同时检查 FK + M2M 两路部门归属 |
| 前端部门页 | `frontend/app/departments/page.tsx` → 成员管理模态框 |
| 前端用户页 | `frontend/app/users/page.tsx` → 部门选择器 |
| 前端 KB 权限 | `frontend/app/knowledge-bases/page.tsx` → 按角色/部门/用户分配 7 种权限 + 用户级覆盖 |

---

## 10. 会话管理链路

| 步骤 | 位置 |
|------|------|
| 会话创建 | `enterprise_rag.py`/`personal_rag.py` → `_save_session()` → `ChatSession` 表 |
| 消息保存 | `ChatMessage`(role=user) + `ChatMessage`(role=assistant) + `RagAnswerSource`(≤10条来源) |
| 会话列表 | `backend/app/api/sessions.py` → `list_sessions()` `GET /api/sessions` |
| 会话详情 | `sessions.py` → `get_session()` `GET /api/sessions/{id}` |
| 消息反馈 | `sessions.py` → `feedback()` `POST /api/sessions/messages/{id}/feedback` (like/dislike) |
| 前端列表 | `frontend/components/chat/session-list.tsx` |

---

## 11. 监控指标链路

```
record_timing / increment_counter → 内存存储 → API 暴露 → 前端 5s 轮询
```

| 步骤 | 位置 |
|------|------|
| 指标收集 | `backend/app/services/metrics_service.py` → `record_timing()` / `increment_counter()` / `track_time()` |
| 线程安全 | `threading.Lock()` 保护 `_timings` / `_counters` |
| 数据裁剪 | 每种 timing 最多保留 1000 条 |
| 查询节点 | `langgraph_workflow.py` → 记录 `rag_rewrite_ms` / `rag_retrieve_ms` / `rag_rerank_ms` / `rag_check_confidence_ms` / `rag_expand_ms` / `rag_total_ms` |
| 计数器 | `rag_query_total` / `rag_query_low_confidence` / `rag_query_error` |
| Worker 指标 | `workers/main.py` → `doc_parsed` / `chunks_created` / `doc_indexed` / `chunks_embedded` / `chunks_reused` |
| API | `backend/app/api/monitor.py` → `GET /api/admin/monitor` / `POST /reset` |
| 前端 | `frontend/app/monitor/page.tsx` → 4 张卡片 + 耗时表格（avg+p95+进度条） |

**注意：** Worker 和 Backend 是独立进程，各自维护自己的内存指标。RAG 指标在 Backend 进程，文档处理指标在 Worker 进程。

---

## 12. LLM 模型配置链路

| 步骤 | 位置 |
|------|------|
| 配置存储 | `backend/app/db/models/model_config.py` → `ModelConfig` 表 |
| 配置优先 | `llm_service.py` → `_get_db_config()` → 优先 DB 中 `is_default=true` 的配置，回退 `.env` |
| 缓存 | 60 秒内存缓存 `_db_config_cache` |
| 客户端构建 | `llm_service.py` → `_get_llm()` → 根据 `provider` 自动拼 `base_url` |
| 支持提供商 | DeepSeek / Qwen / OpenAI（兼容协议均可） |
| 管理 API | `backend/app/api/model_configs.py` → CRUD，需 `manage_model_config` 权限 |

---

## 13. 知识缺口 + 审计日志

| 功能 | 写入位置 | 读取位置 |
|------|----------|----------|
| 知识缺口 | `enterprise_rag.py`/`personal_rag.py` → 若 `low_confidence=True` 写 `KnowledgeGap` | `backend/app/api/knowledge_gaps.py` |
| 审计日志 | `backend/app/services/audit_service.py` → `log()` | `backend/app/api/audit_logs.py` |

---

## 14. 前端 Think Block 折叠

| 步骤 | 位置 |
|------|------|
| 解析 | `frontend/components/chat/think-block.tsx` → `parseThinkBlocks()` 正则 `/<think>([\s\S]*?)(?:<\/think>\|$)/g` |
| 渲染 | `ThinkBlock` 组件 → 默认折叠，点击展开，`ReactMarkdown` 渲染内容 |
| 应用 | `frontend/components/chat/chat-panel.tsx` → `MessageContent` 对历史消息和流式内容统一使用 |

---

## 15. 技术栈总结

| 层 | 技术 |
|----|------|
| 后端框架 | FastAPI + Pydantic + SQLAlchemy(async) |
| AI 编排 | LangChain + LangGraph (StateGraph) |
| 向量库 | Milvus (IVF_FLAT, COSINE) |
| 数据库 | PostgreSQL 16 + pgvector + pg_trgm |
| 对象存储 | MinIO |
| 缓存/限流 | Redis |
| Embedding | bge-m3 (Docker 内 sentence-transformers) |
| Rerank | bge-reranker-v2-m3 (FlagReranker) |
| LLM | DeepSeek / Qwen / OpenAI（OpenAI 兼容协议） |
| 前端 | Next.js 14 App Router + TailwindCSS + TypeScript |
| 容器化 | Docker Compose |
